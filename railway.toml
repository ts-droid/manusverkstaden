[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && cd server && npm install && npx prisma generate && npx prisma migrate deploy"

[deploy]
startCommand = "cd server && node src/index.js"
