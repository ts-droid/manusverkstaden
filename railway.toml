[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && cd server && npm install && npx prisma generate"

[deploy]
startCommand = "cd server && DATABASE_URL=${DATABASE_PUBLIC_URL:-$DATABASE_URL} npx prisma migrate deploy && DATABASE_URL=${DATABASE_PUBLIC_URL:-$DATABASE_URL} node src/index.js"
